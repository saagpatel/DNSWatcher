# DOC-RECONCILIATION

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which
treats the code as read-only ground truth and edits only documentation so it reflects
the repo's actual current state. No code was changed and no builds or tests were run.
It exists so a batch reviewer can audit exactly what was checked and why each change
was made.

---

## Per-claim findings

### 1 · What it is
**Status: consistent**
Verified by reading: `README.md`, `docs/00-product-brief.md`, `backend/cmd/dnswatcher-api/main.go`,
`backend/internal/trace/engine.go`, `frontend/src/App.tsx`

README, product brief, and architecture doc all describe DNSWatcher as a truth-first DNS trace
viewer that runs a real backend iterative trace from root toward an answer. The entry point
(`backend/cmd/dnswatcher-api/main.go`) and trace engine confirm the backend-run, iterative
tracing claim. The frontend (`App.tsx`) confirms the educational UI with truth note.

---

### 2 · Current state
**Status: drifted** (2 locations in `docs/PORTFOLIO-DISPOSITION.md`)

Evidence: `backend/internal/trace/engine.go` (line 144) emits the truth note
`"qname_minimization_deferred"` with message *"V1 sends the full query name during iterative
tracing; QNAME minimization is deferred."* The README (line 48) and
`docs/01-architecture.md` (line 34) both state the same explicitly.

`docs/PORTFOLIO-DISPOSITION.md` contradicted this in two places:

- **Location 1** — "current state in one paragraph" section:
  Before → `"QNAME minimization is implemented."`
  After  → `"QNAME minimization is **deferred in V1** (full query name sent during iterative tracing; documented explicitly)."`

- **Location 2** — "Last known reference" table, "Distinguishing tech" row:
  Before → `"Real backend iterative trace from root + **QNAME minimization** + …"`
  After  → `"Real backend iterative trace from root + **QNAME minimization deferred in V1 (documented)** + …"`

---

### 3 · Stack
**Status: drifted** (1 line in `docs/01-architecture.md`)

Evidence: `backend/internal/` directory listing (via Glob) contains exactly these packages:
`contracts`, `trace`, `policy`, `httpapi`, `testkit`. No `classify` package exists.
Response-classification helpers (`classifyRole`, `explainRCode`, `isNODATA`) live in
`backend/internal/trace/engine.go`.

`docs/01-architecture.md` "Backend modules" section listed a non-existent module:

Before → `"- \`internal/classify\`: response classification helpers"`
         (also described `internal/trace` as "iterative trace engine and normalization")
After  → line removed; `internal/trace` description updated to
         `"iterative trace engine, normalization, and response classification"`

All other stack claims (Go backend, React + TypeScript + Vite frontend, `github.com/miekg/dns`,
`golang.org/x/time`, Vitest + jsdom test setup) are consistent with `backend/go.mod` and
`frontend/package.json`.

---

### 4 · How to run
**Status: consistent**
Verified by reading: `Makefile`, `frontend/package.json`, `backend/go.mod`

README local workflow (install → generate → test → `npm run dev` → `go run ./cmd/dnswatcher-api`)
maps exactly to Makefile targets and `package.json` scripts. All five steps are defined and
reachable. No missing or renamed targets.

---

### 5 · Known risks (code↔doc contradictions)
**Status: consistent** for docs-to-code; one contract-vs-code discrepancy noted below.

All docs-to-code comparisons are accurate. One cross-layer discrepancy worth a human eye:

**`contracts/openapi.yaml` + `docs/02-api-contract.md` — `hop_purpose: terminal`**
Both the OpenAPI contract and the API contract doc list `"terminal"` as a valid enum value
for `hop_purpose`. Reading `backend/internal/trace/engine.go` exhaustively, the engine only
ever sets `hop_purpose` to `"delegation"`, `"nameserver_address_lookup"`, or `"cname_follow"`.
The value `"terminal"` is never produced. This is a contract-ahead-of-implementation gap.
`docs/02-api-contract.md` was left unchanged because it accurately mirrors `contracts/openapi.yaml`
(which is outside the editable doc scope). The gap should be resolved either by implementing
the `"terminal"` purpose or removing it from the OpenAPI schema.

---

### 6 · Next move
**Status: consistent**
Verified by reading: `docs/06-implementation-plan.md`, `docs/07-acceptance-criteria.md`,
`docs/08-runtime-verification.md`, `docs/09-deployment.md`

Implementation plan Phases 0–4 align with what's built (trace engine, HTTP API, React UI,
same-origin container packaging, runtime verification checklist). No demonstrably incomplete
stubs or missing roadmap items found in the doc-vs-code comparison. The deployment doc
(Render Free / `oregon`) matches the README recommendation and `render.yaml` presence.

---

## Summary counts

| Category       | Count |
|----------------|-------|
| consistent     | 4     |
| drifted-fixed  | 2     |
| unverifiable   | 0     |

---

## Footer

Run date/time: 2026-05-30 03:53:17 PDT
Branch: docs/truth-up-2026-05-30
HEAD sha reconciled against: 07d1935af2a21b74679e7682db81e342c9843b81
